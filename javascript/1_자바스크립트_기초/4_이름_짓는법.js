/**
 * Naming Conventions
 *
 * 변수 이름 지을 때
 * 1) 일반적으로 영어를 사용하며 문자와 숫자 모두 사용 할 수 있다.
 * 2) 특수기호는 언더스코어(_)와 달러($)를 사용 할 수 있다.
 * 3) 숫자로 이름을 시작 할 수 없다.
 *    1Name, 2Age
 * 4) 키워드는 변수명으로 사용 할 수 없다.
 *    var const = 'var';
 */
let name = "KDM";
var $num = 10;
const _num = 20;

/**
 * Naming Conventions 2
 *
 * 1) camelCase - 대부분의 언어에서 많이 사용 -> 추천
 * 2) snake_case - 파이썬같은 언어에서 사용
 * 3) PastelCase - C# 마이크로소프트 계열의 언어에서 사용
 */
